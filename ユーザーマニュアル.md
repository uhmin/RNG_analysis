# 乱数評価アプリケーション ユーザーマニュアル

## 1. はじめに
本アプリケーションは、プログラムが生成する「乱数（でたらめな数字）」が、本当に偏りなくでたらめに生成されているか（一様分布しているか）を、グラフを見ながら直感的に評価・確認するためのツールです。

## 2. 起動方法
1. アプリケーションのフォルダ内にある `index.html`（PC向け）または `mobile.html`（スマホ向け）をWebブラウザで開きます。
2. スマホから `mobile.html` にアクセスし、「ホーム画面に追加」を選択することで、独立したネイティブアプリのように（PWAとして）お使いいただけます。
3. インターネットに接続されていることを確認してください（グラフを描画するための部品をインターネットから取得するため）。

## 3. 画面の見方

画面は大きく分けて以下のパネルで構成されています。

### ① コントロールパネル
*   **Startボタン**: 乱数の生成と評価を開始します。
*   **Stopボタン**: 乱数の生成を一時停止します。
*   **Resetボタン**: これまでのデータをすべて消去し、初期状態に戻します。
*   **Speedスライダー**: 乱数を生成するスピードを調整します（1ms〜100ms）。左に動かすほど速くなります。

### ② 統計情報パネル
現在の数値データを確認できます。
*   **Current Value**: 今回生成された16ビット乱数の中に含まれる「1」の数（0〜16個）。
*   **Count**: これまでに生成した乱数の合計回数。
*   **Mean (Total)**: 全Raw Valueの平均値。
*   **Variance (Window)**: 直近32回分のデータに基づく分散（ばらつき具合）。下部に観測された最小値・最大値が記録されます。
*   **Shannon Entropy**: 乱数の「予測しにくさ」を示す情報量インデックス。下部に最小値が記録されます。

### ③ グラフパネル
*   **左側の折れ線グラフ（推移グラフ）**: 
    *   水色の線: 現在のRaw Value（1の数）。
    *   緑色の実線/破線: 直近32回分の平均値（移動平均）と、その期待値（8.0）。
    *   ピンク色の実線/破線: 直近32回分の分散と、その期待値（4.0）。
    *   黄色の実線/破線: シャノンエントロピーと、その最大値（4.0）。
*   **右側の棒グラフ（分布グラフ）**: 
    *   16ビットの数値を4ビットずつに区切り、0〜15の出現頻度を示したヒストグラムです。
*   **下部のヒストグラム (1000回目以降に出現)**: 
    *   試行回数が1000回を超えると、それ以降に観測された Variance と Entropy の値がそれぞれどの程度バラけているかをグラフ化します。

## 4. 操作手順
1.  **Startボタン** をクリックします。グラフが動き始めます。
2.  速すぎる/遅すぎる場合は、**Speedスライダー** を左右に動かして調整してください。
3.  グラフの動きや数値を観察し、1000回目以降に現れる下部ヒストグラムも確認します。
4.  動きを止めたい場合は **Stopボタン** をクリックします。
5.  最初からやり直したい場合は **Resetボタン** をクリックします。

## 5. 乱数の評価方法（良い乱数かどうかの見分け方）

本アプリケーションを使用して、以下のポイントを確認することで乱数の品質を評価できます。

*   **平均値 (Mean) と 分散 (Variance)**
    グラフ上の「薄い破線の基準線（平均は8.0、分散は4.0）」の近くを、実線が細かく上下に行ったり来たりしていれば正常です。基準線から大きく外れたまま戻ってこない場合は、乱数に偏りがあります。
*   **シャノンエントロピー (Shannon Entropy)**
    黄色の実線が、薄い黄色の破線（最大値 4.0）のすぐ下を沿うように動いていれば、予測不可能な良い乱数です。
*   **分布グラフ（右側の棒グラフ）**
    古いデータが減衰して消えていくため、常にうねるように動きます。極端に「1本の棒だけが常に伸び続ける」といった状態にならなければ、良好な乱数と評価できます。
*   **観測値ヒストグラム（下部）**
    Varianceのグラフは4.0を中心に山なりになり、Entropyのグラフは4.0（右端）に向かって山が偏るのが理想的です。
