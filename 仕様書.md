# 乱数評価アプリケーション 仕様書

## 1. システム概要
本システムは、JavaScriptの標準乱数生成機能（`Math.random()`）によって生成された乱数の性質（偏り、ばらつき、分布）をリアルタイムで統計処理し、視覚的に評価・分析するためのWebアプリケーションである。

## 2. 動作環境
*   **対応ブラウザ**: Google Chrome, Mozilla Firefox, Apple Safari, Microsoft Edge の各最新版
*   **必須環境**: JavaScriptが有効になっており、インターネット接続があること（Chart.jsをCDNから取得するため）

## 3. 機能仕様

### 3.1. 乱数生成機能
*   0以上1未満（0.0 <= x < 1.0）の一様乱数を生成する。
*   生成間隔（スピード）は、ユーザーが任意に設定可能である。

### 3.2. 統計計算機能
生成された乱数データをもとに、以下の統計指標をリアルタイムに計算する。
*   **総生成回数 (Count)**: 乱数を生成した合計回数。
*   **全体平均 (Mean Total)**: 実行開始から現在までに生成された全乱数の平均値。
*   **移動平均 (Moving Average)**: 直近50回分の乱数の平均値。局所的な偏りを評価する。
*   **区間標準偏差 (Window Standard Deviation)**: 直近50回分の乱数の標準偏差。局所的なばらつきを評価する。
*   **度数分布 (Distribution)**: 0.0〜1.0の区間を0.1刻みで10分割し、各区間に出現した乱数の回数をカウントする。ただし、直近の偏りを強調するため、値の追加毎に既存の分布に減衰係数（0.995）を掛ける指数加重移動平均（EWMA）的な処理を行っている。
*   **シャノンエントロピー (Shannon Entropy)**: 上記の減衰処理が適用された度数分布の割合から情報量インデックス（シャノンインデックス）を計算し、現在の乱数の「予測しにくさ（一様性）」を評価する。

### 3.3. グラフ描画機能
計算された統計指標を、Chart.jsを用いてリアルタイムにグラフ化する。
*   **推移グラフ (折れ線グラフ)**: 乱数の生の値、移動平均、区間標準偏差、シャノンエントロピーの時間的推移を描画する。また、評価の基準となる以下の期待値（理論値）を破線で描画する。
    *   移動平均の期待値: 0.5
    *   区間標準偏差の期待値: 約0.289 ($1/\sqrt{12}$)
    *   シャノンエントロピーの最大値: 約3.32 ($\log_2(10)$)
*   **分布グラフ (棒グラフ)**: 10分割した区間ごとの出現度数（減衰適用済み）を描画する。比較を容易にするため、Y軸の最大値は固定（35）している。

## 4. 内部モジュール（クラス）構成

### 4.1. `RNG` クラス
*   **役割**: 乱数生成器のラッパー。
*   **メソッド**: 
    *   `next()`: 新しい乱数を1つ生成して返す。

### 4.2. `Statistics` クラス
*   **役割**: 統計値の計算とデータ保持。
*   **プロパティ**: 
    *   `windowSize`: 移動平均などの計算に用いる直近データの保持数（デフォルト50）。
    *   `values`: 直近データの配列。
    *   `totalSum`: 全データの合計値。
    *   `totalCount`: 全データの生成回数。
    *   `distribution`: 10区間の度数分布を保持する配列。
*   **メソッド**:
    *   `add(value)`: 新しい値を追加し、各状態を更新する。分布配列に対しては0.995の減衰処理を行う。
    *   `getMean()`, `getWindowMean()`, `getWindowStdDev()`, `getDistribution()`, `getShannonEntropy()`: 各種統計値を取得する。

### 4.3. `App` クラス
*   **役割**: アプリケーション全体の制御（UIイベント処理、タイマー制御、グラフ更新）。
*   **プロパティ**:
    *   `speed`: 乱数生成のインターバル時間（ミリ秒）。
*   **メソッド**:
    *   `start()`, `stop()`, `reset()`: アプリケーションの実行状態を制御する。
    *   `tick()`: 定期実行されるメインループ。乱数生成、統計更新、UI更新を呼び出す。

## 5. 使用技術・ライブラリ
*   **HTML5 / CSS3**: UIの構造とデザイン（レスポンシブ対応）。
*   **Vanilla JavaScript (ES6+)**: アプリケーションロジック。
*   **Chart.js (v3系以降)**: データの視覚化（CDN経由で読み込み）。
