# 乱数評価アプリケーション 仕様書

## 1. システム概要
本システムは、生成された乱数（16ビットのBool列・整数）の性質（偏り、ばらつき、分布）をリアルタイムで統計処理し、視覚的に評価・分析するためのWebアプリケーションである。

## 2. 動作環境
*   **対応ブラウザ**: Google Chrome, Mozilla Firefox, Apple Safari, Microsoft Edge の各最新版
*   **必須環境**: JavaScriptが有効になっており、インターネット接続があること（Chart.jsをCDNから取得するため）。
*   **PWA対応**: スマホ等のホーム画面に追加することで、ネイティブアプリのように全画面で動作可能。

## 3. 機能仕様

### 3.1. 乱数生成機能
*   0から65535までの16ビット整数をランダムに生成する。
*   生成間隔（スピード）は、ユーザーが任意に設定可能である（1ms〜100ms）。

### 3.2. 統計計算機能
生成された16ビット整数をもとに、以下の統計指標をリアルタイムに計算する。
*   **Raw Value (1の数)**: 16ビットの中に「1」がいくつ含まれているか（0〜16個）。
*   **総生成回数 (Count)**: 乱数を生成した合計回数。
*   **全体平均 (Mean Total)**: 実行開始から現在までに生成された全Raw Valueの平均値。
*   **移動平均 (Moving Average)**: 直近32回分のRaw Valueの平均値。局所的な偏りを評価する。
*   **区間分散 (Window Variance)**: 直近32回分のRaw Valueの分散。局所的なばらつきを評価する。
*   **度数分布 (Distribution)**: 16ビットを4ビットずつ（0000〜1111）の4つの塊に区切り、それぞれを0〜15の整数として16区間でカウントする。直近の偏りを強調するため、既存の分布に減衰係数（0.99）を掛ける指数加重移動平均（EWMA）的な処理を行っている。
*   **シャノンエントロピー (Shannon Entropy)**: 上記の減衰処理が適用された度数分布から情報量インデックスを計算し、現在の乱数の「予測しにくさ（一様性）」を評価する。

### 3.3. グラフ描画機能
計算された統計指標を、Chart.jsを用いてリアルタイムにグラフ化する。
*   **推移グラフ (折れ線グラフ)**: 乱数の生の値（Raw Value）、移動平均、区間分散、シャノンエントロピーの時間的推移を描画する。また、評価の基準となる以下の期待値（理論値）を破線で描画する。
    *   移動平均の期待値: 8.0 (16ビットの半分の確率)
    *   区間分散の期待値: 4.0 ($16 \times 0.5 \times 0.5$)
    *   シャノンエントロピーの最大値: 4.0 ($\log_2(16)$)
*   **分布グラフ (棒グラフ)**: 16分割した区間ごとの出現度数（減衰適用済み）を描画する。
*   **ヒストグラム**: 1000回の試行後から、観測された分散およびエントロピーの値のヒストグラムを画面下部に集計・描画する。

## 4. 内部モジュール（クラス）構成

### 4.1. `RNG` クラス
*   **役割**: 乱数生成器のラッパー。
*   **メソッド**: 
    *   `next()`: 16ビットランダム整数を生成して返す。

### 4.2. `Statistics` クラス
*   **役割**: 統計値の計算とデータ保持。
*   **プロパティ**: 
    *   `windowSize`: 移動平均などの計算に用いる直近データの保持数（32）。
    *   `values`: 直近データの配列。
    *   `totalSum`: 全データの合計値。
    *   `totalCount`: 全データの生成回数。
    *   `distribution`: 16区間の度数分布を保持する配列。
*   **メソッド**:
    *   `add(value)`: 新しい値を追加し、各状態を更新する。分布配列に対しては0.99の減衰処理を行う。
    *   `getMean()`, `getWindowMean()`, `getWindowStdDev()`, `getDistribution()`, `getShannonEntropy()`: 各種統計値を取得する。

### 4.3. `App` クラス
*   **役割**: アプリケーション全体の制御（UIイベント処理、タイマー制御、グラフ更新）。
*   **プロパティ**:
    *   `speed`: 乱数生成のインターバル時間（ミリ秒）。
*   **メソッド**:
    *   `start()`, `stop()`, `reset()`: アプリケーションの実行状態を制御する。
    *   `tick()`: 定期実行されるメインループ。乱数生成、統計更新、UI更新を呼び出す。

## 5. 使用技術・ライブラリ
*   **HTML5 / CSS3**: UIの構造とデザイン（PC用およびスマホ用のレスポンシブ対応）。
*   **Vanilla JavaScript (ES6+)**: アプリケーションロジック。
*   **Chart.js (v3系以降)**: データの視覚化（CDN経由で読み込み）。
*   **Service Worker / Web App Manifest**: PWA対応のためのオフラインキャッシュおよび設定機能。
